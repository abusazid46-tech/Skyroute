# Prevent directory browsing
Options -Indexes

# Protect admin page
<Files "admin.html">
    AuthType Basic
    AuthName "Admin Access"
    AuthUserFile /path/to/.htpasswd
    Require valid-user
</Files>
